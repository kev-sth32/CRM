const MODES=['copilot','assisted','autonomous'];function validateAgentConfig(input){if(!input?.name)throw Error('name is required');if(input.mode&&!MODES.includes(input.mode))throw Error('invalid agent mode');return {name:String(input.name).trim(),mode:input.mode||'copilot',language:input.language||'en',tone:input.tone||'professional',system_policy:input.system_policy||null,allowed_tools:Array.isArray(input.allowed_tools)?input.allowed_tools:[],escalation_rules:input.escalation_rules||{}}}module.exports={validateAgentConfig,MODES};
