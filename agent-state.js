const allowed={draft:['active'],active:['paused','disabled'],paused:['active','disabled'],disabled:[]};function transition(current,next){if(!allowed[current]?.includes(next))throw Error(`Invalid transition: ${current} to ${next}`);return next}function canActivate({mode,hasEscalationRules,policyMode}){return mode!=='autonomous'||(hasEscalationRules&&['assisted','autonomous'].includes(policyMode))}module.exports={transition,canActivate};
