const assert=require('assert');const {guardAction}=require('./ai-guardrails');(async()=>{let called=0;const pool={query:async()=>{called++;return{rows:[{id:'approval-1',status:'pending'}]}}};const pending=await guardAction({pool,policy:{agent_mode:'copilot',approval_required:['send_message']},tenantId:'t1',userId:'u1',action:'send_message',payload:{}});assert.equal(pending.execute,false);assert.equal(pending.approval.status,'pending');const allowed=await guardAction({pool,policy:{agent_mode:'autonomous',approval_required:[]},tenantId:'t1',userId:'u1',action:'create_task',payload:{}});assert.equal(allowed.execute,true);assert.equal(called,1);console.log('AI guardrails test passed')})().catch(e=>{console.error(e);process.exit(1)});
