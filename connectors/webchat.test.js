const assert=require('assert');const {normalizeWebChat}=require('./webchat');const event=normalizeWebChat({session_id:'s1',message_id:'m1',message:'Hello',sender:{name:'Sita'}});assert.equal(event.channel,'website_chat');assert.equal(event.external_id,'s1-m1');assert.equal(event.text,'Hello');assert.equal(normalizeWebChat({message:'missing session'}),null);console.log('Website chat normalization test passed');
