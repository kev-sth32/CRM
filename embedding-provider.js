class EmbeddingProvider{constructor(adapter=null){this.adapter=adapter}async embed(texts){if(!this.adapter)throw Error('No embedding provider configured');if(!Array.isArray(texts)||!texts.length)throw Error('texts are required');return this.adapter.embed(texts)}}async function indexChunks(pool,vectorStore,provider,documentId,tenantId){const r=await pool.query('SELECT id,chunk_index,content,metadata FROM knowledge_chunks WHERE document_id=$1 AND tenant_id=$2 ORDER BY chunk_index',[documentId,tenantId]);const vectors=await provider.embed(r.rows.map(x=>x.content));await vectorStore.upsert(r.rows.map((x,i)=>({tenant_id:tenantId,chunk_id:x.id,embedding:vectors[i],metadata:x.metadata})));return {indexed:r.rows.length}}module.exports={EmbeddingProvider,indexChunks};
