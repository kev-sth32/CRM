const assert=require('assert');const {protectedTool}=require('./protected-tool');(async()=>{let ran=false;const pool={query:async()=>({rows:[{id:'a1',status:'pending'}]})};const t=protectedTool({pool,action:'send_message',tool:{name:'send_message',permission:'messages.send',execute:async()=>{ran=true;return{sent:true}}}});const x=await t.execute({body:'Hi'},{tenant_id:'t1',user_id:'u1',permissions:['messages.send'],policy:{agent_mode:'copilot',approval_required:['send_message']}});assert.equal(x.status,'approval_required');assert.equal(ran,false);console.log('Protected tool test passed')})().catch(e=>{console.error(e);process.exit(1)});
