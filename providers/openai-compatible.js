class OpenAICompatibleProvider{constructor({apiKey,baseUrl='https://api.openai.com/v1',model='gpt-4o-mini'}={}){this.apiKey=apiKey;this.baseUrl=baseUrl.replace(/\/$/,'');this.model=model}async generate(request){if(!this.apiKey)throw Error('AI provider API key is required');const r=await fetch(`${this.baseUrl}/chat/completions`,{method:'POST',headers:{'content-type':'application/json',authorization:`Bearer ${this.apiKey}`},body:JSON.stringify({model:this.model,messages:[{role:'system',content:request.system||''},...(request.messages||[])],temperature:0.2})});if(!r.ok)throw Error(`AI provider returned ${r.status}`);const x=await r.json();return {text:x.choices?.[0]?.message?.content||'',model:x.model||this.model,usage:x.usage||null}}}module.exports={OpenAICompatibleProvider};
