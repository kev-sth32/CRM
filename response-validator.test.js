const assert=require('assert');const {validateGroundedResponse}=require('./response-validator');const s=[{id:'c1'}];assert.equal(validateGroundedResponse({text:'Answer',citations:['c1']},s).valid,true);assert.equal(validateGroundedResponse({text:'Answer',citations:['bad']},s).valid,false);assert.equal(validateGroundedResponse({text:'Answer'},s).reason,'missing_citations');console.log('Grounded response validation test passed');
