class VectorStore{constructor(adapter=null){this.adapter=adapter}async upsert(items){if(!this.adapter)throw Error('No vector store configured');return this.adapter.upsert(items)}async search({tenant_id,query,limit=8}){if(!this.adapter)throw Error('No vector store configured');if(!tenant_id)throw Error('Tenant context required');return this.adapter.search({tenant_id,query,limit})}}module.exports={VectorStore};
